int[] counts = new int[10]; // declare and initialize the array
Arrays.fill(counts, 0); // set all elements to zero using the fill() method from the Arrays class

int[] bonus = new int[15]; // declare and initialize the array
for (int i = 0; i < bonus.length; i++) {
    bonus[i] += 1;
}
int[] bestScores = {10, 20, 30, 40, 50}; // declare and initialize the array
for (int i = 0; i < bestScores.length; i++) {
    System.out.println(bestScores[i]);
}
